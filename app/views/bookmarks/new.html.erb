<h1>Add your movie to <%= @list.name %></h1>

<%= simple_form_for([@list, @bookmark]) do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment %>
  <%= f.submit "Add Movie", class: 'btn btn-success' %>
<% end %>
